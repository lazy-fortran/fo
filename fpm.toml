name = "fo"
version = "0.3.2"
license = "MIT"
author = "Christopher Albert"
maintainer = "albert@tugraz.at"
copyright = "Copyright 2025, Christopher Albert"
description = "Fortran build cache, incremental rebuild, affected-test selection, MCP server"

[build]
auto-executables = true
auto-tests = true
auto-examples = false
module-naming = false

[install]
library = false

[dependencies]
fx = { git = "https://github.com/lazy-fortran/fx.git", branch = "main" }
fortfront = { git = "https://github.com/lazy-fortran/fortfront.git", tag = "v0.1.1" }
openmp = "*"

[fortran]
implicit-typing = false
implicit-external = false
source-form = "free"
